version: "3.5"

services:
    app:
        container_name: app
        build: .

    nginx:
        image: nginx:1.26.0
        volumes:
            - ./nginx.conf:/etc/nginx/nginx.conf:ro
        ports:
            - 9999:9999
        depends_on:
            - app
